# Runtime dependencies for the Streamlit dashboard (dashboard.py).
# Deliberately minimal and unpinned on the patch: Streamlit Community Cloud builds on
# Linux and on an older Python than this project's local 3.14 venv, so the full local
# pin set (requirements-lock.txt) will not resolve there - it contains pywin32.
streamlit>=1.40
pandas>=2.2
altair>=5.4
alpaca-py>=0.42
python-dotenv>=1.0
